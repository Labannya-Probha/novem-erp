import Reportmodule from '../../../pages/Reportmodule'

export default function ReportViewer({ userName, userId, role, company }) {
  return <Reportmodule userName={userName} userId={userId} role={role} company={company} />
}
