import ConsumptionEntry from '../../../pages/ConsumptionEntry.jsx'

export default function ConsumptionEntryTab({ userName, isAdmin }) {
  return <ConsumptionEntry userName={userName} isAdmin={isAdmin} />
}
