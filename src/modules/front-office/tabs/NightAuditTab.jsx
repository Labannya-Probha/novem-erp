import NightAudit from '../../../pages/NightAudit.jsx'

export default function NightAuditTab({ userName, isAdmin, role }) {
  return <NightAudit userName={userName} isAdmin={isAdmin} role={role} />
}
